# Issue tracker

This repository uses GitHub Issues for work tracking.

- Repository: `shariiq/animesource-web`
- CLI: `gh`
- Create issues with `gh issue create`.
- Read and update issues with the GitHub CLI.
- Pull requests are not included as a triage request surface by default.
